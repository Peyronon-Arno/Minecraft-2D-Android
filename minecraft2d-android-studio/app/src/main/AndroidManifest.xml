<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.iut.minecraft2d">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"

        android:supportsRtl="true"
        android:theme="@style/Theme.Minecraft2D" >

        <activity android:name=".activity_class.MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <activity android:name=".activity_class.PlayingActivity" android:screenOrientation="userLandscape" />
        <activity android:name=".activity_class.Generation"/>
        <activity android:name=".activity_class.ListItem"/>


    </application>

</manifest>